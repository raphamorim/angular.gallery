'use strict';

// Declare app level module which depends on filters, and services
var gallery = angular.module('gallery', [
    'myApp.controllers',
    'myApp.filters',
    'myApp.services',
    'myApp.directives'
    // ,'ngRoute'
]);
